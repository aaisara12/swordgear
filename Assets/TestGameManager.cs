using UnityEngine;

/// <summary>
/// Legacy round-loop test harness (3 combat levels + shop). Retired in commit 14 — use
/// <see cref="LinearRunPreviewTest"/> and the Map/Arena linear run flow instead.
/// Kept as an empty component so old test scenes keep valid script references.
/// </summary>
public class TestGameManager : MonoBehaviour
{
}
